<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quiz">

    <application
        android:allowBackup="true"
        android:label="Quiz"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quiz">

        <activity android:name="com.example.quiz.CalculatorActivity"
            android:exported="false" />
        <activity android:name="com.example.quiz.ResultActivity"
            android:exported="false" />
        <activity android:name="com.example.quiz.QuizActivity"
            android:exported="false" />
        <activity android:name="com.example.quiz.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
